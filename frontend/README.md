# frontend (opcional — PLUS)

UI mínima opcional. **No bloquea ningún MVP** (CLAUDE.md sección 3).

Si hay tiempo al final del hackathon: páginas Jinja2 servidas por el `gateway` o
un Vite + React muy simple. Mientras tanto, la demo se hace con `scripts/demo.http`
y `scripts/seed.py`.
